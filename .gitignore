# Python-generated files
__pycache__/
*.py[oc]
bin/
build/
build-linux/
__build/
__install/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Claude Code: settings.json is shared, but per-machine permission grants and the
# scheduler lock are local state.
.claude/settings.local.json
.claude/scheduled_tasks.lock
